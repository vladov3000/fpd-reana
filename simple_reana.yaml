version: 0.6.0
inputs:
  files:
    - inputs/
  parameters: {$ref: /inputs/input.yml}
workflow:
  type: 'yadage'
  file: 'simple_scatter.yaml'
outputs:
  files:
    - simple_scatter_*/echoflow/output.txt
